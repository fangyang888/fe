# 异常处理、中间件和生命周期

## 异常处理

全局异常处理器把领域异常转换成稳定HTTP结构；未知异常记录详细日志，但对外只返回安全信息。

## 中间件

中间件适合跨接口行为：请求ID、访问日志、耗时、CORS和安全Header。业务意图识别不应写在中间件中。

## Lifespan

应用启动时创建连接池、检查必要配置；关闭时释放数据库、Redis和模型客户端。不要依赖进程突然退出时自动清理。

## 练习

增加请求ID中间件，在响应Header返回ID；再写测试证明客户端传入合法ID时能够沿用。

## 验收

能区分请求级清理和应用级清理，并确保异常路径仍会释放资源。
