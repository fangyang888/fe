# App.jsx 技能清单 (Skills)

## 概述

`App.jsx` 是一个彩票预测器组件 (`LotteryPredictor`)，包含多种预测算法和数据分析功能。

---

## 核心功能模块

### 1. 基础数学工具

| 函数名              | 说明             |
| ------------------- | ---------------- |
| `sigmoid(x)`        | Sigmoid 激活函数 |
| `dot(w, x)`         | 向量点积运算     |
| `clamp(v)`          | 数值范围限制     |
| `linearFit(xs, ys)` | 线性拟合         |

---

### 2. 数据解析

| 函数名         | 说明         |
| -------------- | ------------ |
| `parseInput()` | 解析输入数据 |

---

### 3. 正向预测算法

| 函数名              | 说明                                |
| ------------------- | ----------------------------------- |
| `predictB(history)` | 预测算法 B                          |
| `predictC(history)` | 预测算法 C                          |
| `predictM(history)` | 机器学习预测（逻辑回归）            |
| `predictI(history)` | 预测算法 I                          |
| `predictN(history)` | 反预测算法：预测不出现的数字        |
| `predictX(history)` | 规则X：排除上一行数字，选择高频数字 |
| `predictL(history)` | 学习算法：多特征综合预测            |

---

### 4. 杀码算法 (Kill Number Predictions)

| 函数名                        | 说明                                            |
| ----------------------------- | ----------------------------------------------- |
| `predictK1(history)`          | 马尔可夫链反向预测 - 找转移概率最低的数字       |
| `predictK2(history)`          | 周期性排除 - 刚出现的数字下期大概率不出         |
| `predictK3(history)`          | 连续排除法 - 连续多期出现的数字下期不出概率增加 |
| `predictK4(history)`          | 差值反推 - 基于相邻两期差值模式预测             |
| `predictK5(history)`          | 反共现分析 - 找与上一行很少一起出现的数字       |
| `learnKillWeights(history)`   | 长期学习杀码权重 - 自动学习算法权重             |
| `predictKillNumbers(history)` | 综合杀码推荐 - 结合所有杀码算法                 |

---

### 5. 尾数预测算法 (Tail Prediction)

`predictTail(history)` - 可学习的尾数预测器

#### 子算法：

| 函数名                               | 说明                         |
| ------------------------------------ | ---------------------------- |
| `runT1(tailsData, idx)`              | 一阶马尔可夫转移             |
| `runT2(tailsData, idx)`              | 二阶马尔可夫转移（看前两期） |
| `runT3(historyData, tailsData, idx)` | 和值尾数关联                 |
| `runT4(tailsData, idx)`              | N-gram 序列匹配（看前3期）   |
| `runT5(tailsData, idx)`              | 差值模式分析                 |
| `runT6(tailsData, idx)`              | 周期分析（尾数出现周期）     |
| `runT7(tailsData, idx)`              | 冷热平衡（遗漏值回补）       |
| `runT8(tailsData, idx)`              | 012路补偿                    |
| `learnWeights()`                     | 回测学习权重                 |
| `getKillSet()`                       | 杀码逻辑                     |
| `getTopContributors(digit)`          | 找出对每个数字贡献最大的算法 |

---

### 6. 生肖预测

| 函数名            | 说明                                                        |
| ----------------- | ----------------------------------------------------------- |
| `predictZodiac()` | 预测下一个生肖（二阶马尔可夫 + 遗漏回补 + 频率 + 邻近偏好） |

---

### 7. 机器学习工具

| 函数名                        | 说明             |
| ----------------------------- | ---------------- |
| `buildFeatures(history, num)` | 构建特征向量     |
| `trainLogistic(history)`      | 训练逻辑回归模型 |

---

### 8. 统计分析

| 函数名                         | 说明                         |
| ------------------------------ | ---------------------------- |
| `computeHotCold(history)`      | 计算热号冷号                 |
| `buildChart(history)`          | 构建图表数据                 |
| `calculateStatistics(history)` | 统计分析（最后18行对比验证） |
| `calculateSummary(history)`    | 汇总分析（推荐不出现的数字） |

---

### 9. AI 推荐

| 函数名                                                           | 说明                                                   |
| ---------------------------------------------------------------- | ------------------------------------------------------ |
| `selectFromCurrentPredictions(currentResults, summary, history)` | 🤖 AI 独立思考推荐算法（结合机器学习权重、启发式规则） |

---

### 10. 数据管理

| 函数名                             | 说明               |
| ---------------------------------- | ------------------ |
| `loadHistory()`                    | 加载历史数据       |
| `saveHistoryToFile(historyString)` | 保存历史数据到文件 |
| `runPrediction(flag)`              | 运行预测           |

---

## 文件信息

- **文件路径**: `/Users/yang/fe/fe/src/App.jsx`
- **总行数**: 2965 行
- **主组件**: `LotteryPredictor`
- **依赖**:
  - `react`
  - `NumberDigitPredictor.jsx`
  - `result.ts` (生肖历史数据)
